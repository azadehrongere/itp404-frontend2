{{outlet}}




{{#each model as |task|}}
	<div class="well">
		<h2>{{task.type}}</h2>
		<h4>{{#link-to 'tasks.edit' task.id}}{{task.title}}{{/link-to}}</h4>
		<small>Event date: {{formatdate task.date}}</small>
		<p>{{task.description}}</p>
		
	</div>
{{/each}}